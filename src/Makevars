#####  Compiler flags  #####
USE_FC_TO_LINK =
PKG_FFLAGS = $(SHLIB_OPENMP_FFLAGS) -cpp
PKG_LIBS = $(SHLIB_OPENMP_FFLAGS) -cpp


#####  Phony target for R's build system to invoke  #####
all: $(SHLIB) clean

#####  Clean target  #####
clean:
	rm -f *.o *.mod
